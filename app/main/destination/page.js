import {metadata} from "../../(shared)/heading/Heading"


const Destination =()=>{
    return (
       <section className='  flex justify-center items-center md:items-start h-screen p-8'>
           <h1 className=' text-[40px] text-black font-bold text-center '>This is the Destination Page</h1>
       </section>
    )
}

export default Destination;
metadata.title= 'Destination'

export {metadata};